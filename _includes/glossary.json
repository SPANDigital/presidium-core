{
  {% for article in site.glossary %}
  "{{article.title}}": {
    "url": "{{ article.url }}",
    "content": "{{ article.content | strip_newlines | replace: '"','\"' }}"
  }{% if forloop.last %}{% else %},{% endif %}
  {% endfor %}
}